<Window
    x:Class="NiueryToolKit.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:extensions="clr-namespace:NiueryToolKit.Extension.Markup"
    xmlns:local="clr-namespace:NiueryToolKit"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:NiueryToolKit.ViewModel.MainWindow;assembly=NiueryToolKit.ViewModel"
    Title="Niuery Toolkit"
    Width="800"
    Height="450"
    d:DataContext="{d:DesignInstance Type=vm:MainViewModel}"
    Icon="{StaticResource AppIconSource}"
    WindowStartupLocation="CenterScreen"
    WindowState="Normal"
    WindowStyle="SingleBorderWindow"
    mc:Ignorable="d">

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="1*" />
            <RowDefinition Height="1*" />
            <RowDefinition Height="1*" />
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>

        <Button
            Margin="20"
            Command="{Binding ShowWindowCommand}"
            CommandParameter="FileHASH"
            Content="{extensions:I18n Key=文件HASH}" />

        <Button
            Grid.Column="1"
            Margin="20"
            Command="{Binding SwitchLanguageCommand}"
            Content="{extensions:I18n Key=语言切换}" />
        <Button
            Grid.Column="2"
            Margin="20"
            Command="{Binding ShowWindowCommand}"
            CommandParameter="ImageProcessing"
            Content="{extensions:I18n Key=图片处理}" />
    </Grid>
</Window>
